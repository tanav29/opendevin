Remove the only chat mode so user can only make sandboxed agents
There must be folders for github repos that contsist of session that are not archived
All have the same repo for sandbox 
maybe a diff page also like /p/<projectid> that has
- A kandbox ui for that folder with working sesion and completed once
- Create a session with jsut a prompt 
- A list view instead taht shows all the sessions
- So basicallly session will shown in a group callled folder
- each session will have its own sandbox 

## THIS WILL BE THE BEST ##
