We prefer minimal code edits that just works never repeat code or bad code

We have next js frontend `apps/frontend` and hono backend `apps/backend` with drizzle and better auth - github auth

We uses convex as db https://convex.dev

We use pnpm

Dont edit shadcn files in components/ui/
